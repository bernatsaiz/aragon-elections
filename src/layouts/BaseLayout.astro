---
import Player from "../components/Player.astro";
import { episodes } from "../data/podcast";

interface Props {
  title?: string;
  showSidebar?: boolean;
  noindex?: boolean;
}

const { title = "Elecciones en Aragón", showSidebar = true, noindex = false } = Astro.props;
const pathname = Astro.url.pathname;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="Podcast sobre las elecciones autonómicas de la Comunidad Autónoma de Aragón" />
    <meta name="generator" content={Astro.generator} />
    {noindex && <meta name="robots" content="noindex, nofollow" />}
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=Pixelify+Sans:wght@400;500;600;700&family=Jersey+10&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-25..200" rel="stylesheet" />
    <style is:global>
      :root {
        --bg-base: #0a0a0a;
        --bg-elevated: #121212;
        --bg-sidebar: #000000;
        --bg-card: #181818;
        --bg-hover: #282828;
        --text-primary: #ffffff;
        --text-secondary: #b3b3b3;
        --text-muted: #6a6a6a;
        --accent: #1db954;
        --accent-hover: #1ed760;
        --border: #333;
        --radius: 8px;
        --radius-sm: 4px;
        --sidebar-width: 240px;
        --player-height: 90px;
      }
      *, *::before, *::after { box-sizing: border-box; }
      html { font-family: "DM Sans", system-ui, sans-serif; background: var(--bg-base); color: var(--text-primary); font-size: 16px; line-height: 1.5; }
      body { margin: 0; min-height: 100vh; display: flex; flex-direction: column; }
      a { color: inherit; text-decoration: none; }
      button { font-family: inherit; cursor: pointer; border: none; background: none; color: inherit; padding: 0; }
      ul { list-style: none; margin: 0; padding: 0; }
    </style>
  </head>
  <body>
    <div class="disclaimer-overlay" id="disclaimer-overlay" role="dialog" aria-modal="true" aria-labelledby="disclaimer-title">
      <div class="disclaimer-box">
        <h2 class="disclaimer-title" id="disclaimer-title">Aviso</h2>
        <p class="disclaimer-text">Electionify es un proyecto experimental generado con IA (NotebookLM, Gemini, Cursor) sin vinculación política real. El contenido puede contener errores y no debe utilizarse como información verificada. Al hacer clic en "Aceptar", confirmas que entiendes la naturaleza del proyecto.</p>
        <button type="button" class="disclaimer-btn" id="disclaimer-accept">Aceptar</button>
      </div>
    </div>
    <button type="button" class="sidebar-open-trigger" id="sidebar-open-trigger" aria-label="Abrir menú" aria-expanded="false">
      <span class="material-symbols-outlined sidebar-open-trigger__icon" aria-hidden="true">menu</span>
    </button>
    <div class="sidebar-overlay sidebar-overlay--hidden" id="sidebar-overlay" aria-hidden="true"></div>
    <div class="app" id="app">
      <aside class="sidebar" id="sidebar">
        <button type="button" class="sidebar-toggle" id="sidebar-toggle" aria-label="Cerrar menú" aria-expanded="false">
          <span class="material-symbols-outlined sidebar-toggle__icon" aria-hidden="true">menu</span>
        </button>
        <div class="logo">
          <img src="/logo.svg" alt="Logo" class="logo-img" width="333" height="61" />
        </div>
        <nav class="nav" aria-label="Navegación">
          <a href="/" class={`nav-item ${pathname === "/" ? "nav-item--active" : ""}`}>
            <span class="material-symbols-outlined nav-item__icon" aria-hidden="true">home</span>
            Inicio
          </a>
          <a href="/sobre/" class={`nav-item ${pathname.startsWith("/sobre") ? "nav-item--active" : ""}`}>
            <span class="material-symbols-outlined nav-item__icon" aria-hidden="true">info</span>
            Sobre este proyecto
          </a>
          <a href="/jugar/" class={`nav-item ${pathname.startsWith("/jugar") ? "nav-item--active" : ""}`}>
            <span class="material-symbols-outlined nav-item__icon" aria-hidden="true">sports_esports</span>
            Jugar
          </a>
        </nav>
        <div class="sidebar-section">
          <p class="sidebar-label">Listas de reproducción</p>
          <ul class="sidebar-list">
            <li><span class="sidebar-list-item">Los 50 de España</span></li>
            <li><span class="sidebar-list-item">Música para trabajar</span></li>
            <li><span class="sidebar-list-item">Date vibes</span></li>
            <li><span class="sidebar-list-item">Buen rollo</span></li>
            <li><span class="sidebar-list-item">Chill español</span></li>
            <li><span class="sidebar-list-item">Indie nacional</span></li>
            <li><span class="sidebar-list-item">Conduciendo</span></li>
            <li><span class="sidebar-list-item">Gimnasio</span></li>
            <li><span class="sidebar-list-item">Noche de viernes</span></li>
            <li><span class="sidebar-list-item">Lluvia y café</span></li>
            <li><span class="sidebar-list-item">Acústico en vivo</span></li>
            <li><span class="sidebar-list-item">Éxitos del verano</span></li>
            <li><span class="sidebar-list-item">Concentración</span></li>
            <li><span class="sidebar-list-item">Ruta y carretera</span></li>
            <li><span class="sidebar-list-item">Sofá y manta</span></li>
          </ul>
        </div>
        <div class="sidebar-section">
          <p class="sidebar-label">Escuchados recientemente</p>
          <ul class="sidebar-list sidebar-list--recent">
            <li><span class="sidebar-list-item sidebar-list-item--album">
              <span class="sidebar-list-art"></span>
              <span class="sidebar-list-text">Elecciones en Aragón</span>
            </span></li>
            <li><span class="sidebar-list-item sidebar-list-item--album">
              <span class="sidebar-list-art"></span>
              <span class="sidebar-list-text">Las Cortes de Aragón</span>
            </span></li>
            <li><span class="sidebar-list-item sidebar-list-item--album">
              <span class="sidebar-list-art"></span>
              <span class="sidebar-list-text">Teruel Existe</span>
            </span></li>
          </ul>
        </div>
        <div class="sidebar-footer">
          <p class="sidebar-label">Podcast</p>
          <p class="sidebar-desc">5 episodios · Elecciones autonómicas</p>
        </div>
      </aside>
      <main class="main">
        <slot />
      </main>
    </div>
    <Player episodes={episodes} />
  </body>
</html>

<script>
  (function() {
    const toggle = document.getElementById("sidebar-toggle");
    const openTrigger = document.getElementById("sidebar-open-trigger");
    const overlay = document.getElementById("sidebar-overlay");
    const sidebar = document.getElementById("sidebar");
    if (!sidebar || !overlay || !toggle || !openTrigger) return;

    const isMobile = function() { return window.matchMedia("(max-width: 768px)").matches; };

    function openMobile() {
      sidebar.classList.add("sidebar--open");
      overlay.classList.remove("sidebar-overlay--hidden");
      overlay.setAttribute("aria-hidden", "false");
      toggle.setAttribute("aria-expanded", "true");
      openTrigger.setAttribute("aria-expanded", "true");
      openTrigger.setAttribute("aria-label", "Cerrar menú");
    }
    function closeMobile() {
      sidebar.classList.remove("sidebar--open");
      overlay.classList.add("sidebar-overlay--hidden");
      overlay.setAttribute("aria-hidden", "true");
      toggle.setAttribute("aria-expanded", "false");
      openTrigger.setAttribute("aria-expanded", "false");
      openTrigger.setAttribute("aria-label", "Abrir menú");
    }

    openTrigger.addEventListener("click", function() {
      if (!isMobile()) return;
      if (sidebar.classList.contains("sidebar--open")) closeMobile();
      else openMobile();
    });
    toggle.addEventListener("click", closeMobile);
    overlay.addEventListener("click", closeMobile);

    sidebar.querySelectorAll("a.nav-item").forEach(function(link) {
      link.addEventListener("click", function() {
        if (isMobile()) closeMobile();
      });
    });
  })();

  (function() {
    const DISCLAIMER_KEY = "electionify-disclaimer-accepted";
    const overlay = document.getElementById("disclaimer-overlay");
    const acceptBtn = document.getElementById("disclaimer-accept");
    if (!overlay || !acceptBtn) return;
    var path = window.location.pathname || "/";
    var isHome = path === "/" || path === "" || path === "/index.html";
    if (!isHome) {
      overlay.classList.add("disclaimer-overlay--hidden");
    } else {
      try {
        if (sessionStorage.getItem(DISCLAIMER_KEY)) {
          overlay.classList.add("disclaimer-overlay--hidden");
        }
      } catch (e) {}
    }
    acceptBtn.addEventListener("click", function() {
      try { sessionStorage.setItem(DISCLAIMER_KEY, "1"); } catch (e) {}
      overlay.classList.add("disclaimer-overlay--hidden");
    });
  })();
</script>

<style>
  .disclaimer-overlay {
    position: fixed;
    inset: 0;
    z-index: 99999;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 0, 0, 0.85);
    padding: 24px;
    box-sizing: border-box;
    min-height: 100vh;
    min-height: 100dvh;
  }
  .disclaimer-overlay--hidden {
    display: none !important;
  }
  .disclaimer-box {
    max-width: 480px;
    width: 100%;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px 24px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.5);
  }
  .disclaimer-title {
    font-size: 1.25rem;
    font-weight: 700;
    margin: 0 0 16px;
    color: var(--text-primary);
  }
  .disclaimer-text {
    font-size: 0.9375rem;
    line-height: 1.6;
    color: var(--text-secondary);
    margin: 0 0 24px;
  }
  .disclaimer-btn {
    display: block;
    width: 100%;
    padding: 14px 24px;
    font-size: 1rem;
    font-weight: 600;
    color: #000;
    background: var(--accent);
    border: none;
    border-radius: var(--radius);
    cursor: pointer;
    transition: background 0.2s;
  }
  .disclaimer-btn:hover {
    background: var(--accent-hover);
  }
  @media (max-width: 768px) {
    .disclaimer-overlay {
      padding: 16px;
      padding-left: env(safe-area-inset-left, 16px);
      padding-right: env(safe-area-inset-right, 16px);
    }
    .disclaimer-box {
      padding: 24px 20px;
      max-height: 85vh;
      max-height: 85dvh;
      overflow-y: auto;
    }
    .disclaimer-title { font-size: 1.125rem; margin-bottom: 12px; }
    .disclaimer-text { font-size: 0.9375rem; margin-bottom: 20px; }
    .disclaimer-btn {
      min-height: 48px;
      padding: 16px 24px;
    }
  }

  .app { display: flex; min-height: 100vh; padding-bottom: var(--player-height); }
  /* Botó de menú (burger) — només visible en mobile */
  .sidebar-open-trigger {
    display: none;
    align-items: center;
    justify-content: center;
    position: fixed;
    top: 16px;
    left: 16px;
    z-index: 9999;
    width: 48px;
    height: 48px;
    background: transparent;
    border: none;
    border-radius: 50%;
    color: var(--text-primary);
    cursor: pointer;
    transition: background 0.2s, color 0.2s, transform 0.15s;
  }
  .sidebar-open-trigger:hover {
    background: var(--bg-hover);
    color: var(--text-primary);
    transform: scale(1.05);
  }
  .sidebar-open-trigger__icon {
    font-family: "Material Symbols Outlined", sans-serif;
    font-size: 28px;
    font-variation-settings: "FILL" 0, "wght" 400, "GRAD" 0, "opsz" 28;
    line-height: 1;
    display: inline-block;
  }
  .sidebar-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.5); z-index: 150; transition: opacity 0.2s; }
  .sidebar-overlay--hidden { opacity: 0; pointer-events: none; }
  .sidebar { width: var(--sidebar-width); min-width: var(--sidebar-width); background: var(--bg-sidebar); padding: 24px 0; display: flex; flex-direction: column; gap: 24px; pointer-events: none; transition: transform 0.25s ease; }
  .sidebar * { pointer-events: none; }
  .sidebar .sidebar-toggle, .sidebar .nav-item, .sidebar .sidebar-list-item { pointer-events: auto; cursor: pointer; }
  .sidebar .sidebar-toggle {
    display: none;
    align-items: center;
    justify-content: center;
    align-self: flex-start;
    margin: 0 20px 8px;
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    color: var(--text-secondary);
    transition: background 0.2s, color 0.2s;
  }
  .sidebar .sidebar-toggle:hover { background: var(--bg-hover); color: var(--text-primary); }
  .sidebar-toggle__icon {
    font-family: "Material Symbols Outlined", sans-serif;
    font-size: 24px;
    font-variation-settings: "FILL" 0, "wght" 400, "GRAD" 0, "opsz" 24;
    line-height: 1;
    display: inline-block;
  }
  .logo { padding: 12px 20px; }
  .logo-img { height: 36px; width: auto; max-width: calc(100% - 20px); object-fit: contain; }
  .nav { display: flex; flex-direction: column; gap: 4px; }
  .nav-item { display: flex; align-items: center; gap: 16px; padding: 12px 24px; color: var(--text-secondary); font-weight: 500; transition: color 0.15s, background 0.15s; border-radius: var(--radius-sm); }
  .nav-item:hover { color: var(--text-primary); background: transparent; }
  .nav-item--active { color: var(--text-primary); }
  .nav-item__icon {
    font-family: "Material Symbols Outlined", sans-serif;
    font-size: 24px;
    font-variation-settings: "FILL" 0, "wght" 400, "GRAD" 0, "opsz" 24;
  }
  .nav-item--active .nav-item__icon { font-variation-settings: "FILL" 1, "wght" 400, "GRAD" 0, "opsz" 24; }
  .sidebar-section { padding: 0 12px; }
  .sidebar-label { font-size: 0.75rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.08em; color: var(--text-muted); margin: 0 0 4px; }
  .sidebar-list { display: flex; flex-direction: column; gap: 2px; margin-top: 8px; }
  .sidebar-list-item { display: block; padding: 8px 12px; border-radius: var(--radius-sm); font-size: 0.875rem; color: var(--text-secondary); transition: color 0.15s, background 0.15s; }
  .sidebar-list-item:hover { color: var(--text-primary); background: transparent; }
  .sidebar-list-item--album { display: flex; align-items: center; gap: 12px; }
  .sidebar-list-art { width: 40px; height: 40px; min-width: 40px; border-radius: var(--radius-sm); background: linear-gradient(135deg, var(--accent) 0%, var(--bg-card) 100%); }
  .sidebar-list-text { white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .sidebar-footer { margin-top: auto; padding: 0 24px; padding-top: 16px; border-top: 1px solid var(--border); }
  .sidebar-desc { font-size: 0.8125rem; color: var(--text-secondary); margin: 0; }
  .main { flex: 1; min-width: 0; }

  /* Mobile: només botó burger flotant (cap burger dins la sidebar), espai vertical, sidebar sliding */
  @media (max-width: 768px) {
    .main { padding-top: 72px; }
    .sidebar-open-trigger { display: flex; }
    .sidebar .sidebar-toggle { display: none; }
    .sidebar { position: fixed; left: 0; top: 0; bottom: 0; z-index: 500; transform: translateX(-100%); transition: transform 0.25s ease-out; padding-top: 72px; }
    .sidebar.sidebar--open { transform: translateX(0); }
  }
</style>
